[project]
name = "ipfs-schedule"
authors = ["community"]
description = "Decentralized challenge management system with IPFS integration"
telemetry = false
requirements = []
[contracts.ipfs-challenge]
path = "contracts/ipfs-challenge.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
